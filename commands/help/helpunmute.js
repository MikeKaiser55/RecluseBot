const { MessageEmbed } = require('discord.js');

module.exports = {
    name:'unmute',
    category: 'moderation',
    description: 'Unmutes a muted user ina  server',
    usage: 'unmute <@user>',
    run: async(client, message, args) => {
        
    }
}